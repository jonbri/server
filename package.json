{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "playwright": "npx playwright test",
    "playwright:interactive": "npx playwright test --ui-host=0.0.0.0 --ui-port=8080",
    "docker-run": "./bin/docker-run",
    "docker-stop": "./bin/docker-stop",
    "docker-enter": "./bin/docker-enter"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "@types/node": "^20.8.10"
  },
  "license": "ISC",
  "dependencies": {
    "serve-handler": "^6.1.5"
  }
}
